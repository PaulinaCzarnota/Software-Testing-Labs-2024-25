# Imports the hole function from cheshire.py
from cheshire import hole

# Calls the hole function with an extended message
def rabbit(phrase):
    # Added missing space before "for a very important date"
    hole(phrase + " for a very important date")